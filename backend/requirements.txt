# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# --- HTTP / scraping ---
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2  # backs BeautifulSoup's "xml" parser, used by the eutils scrapers
tenacity==8.5.0

# --- Retrieval ---
# Install torch first from the CPU wheel index (see README), or pip pulls the
# CUDA-bundled wheel. Floors rather than pins because this venv is Python 3.13
# and the original exact pins predate cp313 wheels.
sentence-transformers>=3.0.1
faiss-cpu>=1.9.0.post1
rank_bm25==0.2.2

# --- Classification / ML ---
transformers>=4.45.0
torch>=2.6.0
accelerate>=0.33.0  # required by from_pretrained(low_cpu_mem_usage=True)
datasets==2.20.0
scikit-learn>=1.6.0

# --- Reporting ---
python-docx==1.2.0  # .docx export for GET /verify/{id}/report

# --- Evaluation ---
# Used only by eval/run_evaluation.py, for the confusion-matrix PNG. Not
# imported by the API, so a missing matplotlib cannot affect serving.
matplotlib>=3.9.0

# --- Ops ---
psutil>=6.0.0
